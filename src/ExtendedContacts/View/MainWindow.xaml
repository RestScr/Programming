<Window x:Class="View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:View"
        xmlns:databinding="clr-namespace:View.ViewModel.Converters"
        xmlns:uc="clr-namespace:View.UserControls"
        mc:Ignorable="d"
        Title="Contacts" 
        Height="450" 
        Width="800" 
        Icon="/assets/ContactsApp_96x96.ico">

    <Window.Resources>

        <!-- Оформление элемента в листбоксе -->
        <DataTemplate x:Key="ContactListTemplate">
            <Grid Width="279">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="44*"/>
                    <ColumnDefinition Width="235*"/>
                </Grid.ColumnDefinitions>
                <Image Source="assets/photo_placeholder_100x100.png"
                       Margin="3"
                       />
                <StackPanel 
                    Margin="3,3,3,3" 
                    Grid.Column="1">
                    <TextBlock 
                        FontSize="12"
                        FontWeight="SemiBold"
                        Text="{Binding Name}"/>
                    <TextBlock
                        FontSize="10"
                        Text="{Binding PhoneNumber}"/>
                </StackPanel>
            </Grid>

        </DataTemplate>
    </Window.Resources>

    <!-- Главный интерфейс окна -->
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition 
                Width="100*"
                />
            <ColumnDefinition 
                Width="150*"
                />
        </Grid.ColumnDefinitions>

        <GridSplitter 
            HorizontalAlignment="Left" 
            Height="450" 
            VerticalAlignment="Top" 
            Width="3" Grid.Column="1"/>
        <Border 
            Background="#F8F8F8"
            Grid.Column="0"/>

        <!-- Левая колонка -->
        <Grid Margin="8,8,8,8">
            <Grid.RowDefinitions>
                <RowDefinition Height="33*"/>
                <RowDefinition Height="364*"/>
                <RowDefinition Height="35"/>
            </Grid.RowDefinitions>
            <Grid
                Grid.Row="0"
                Grid.Column="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="41"/>
                    <ColumnDefinition Width="258*"/>
                </Grid.ColumnDefinitions>
                <Label 
                    Content="Find:"
                    Margin="3,3,3,3"
                    Grid.Column="0"
                    />
                <TextBox
                    Grid.Row="0"
                    Grid.Column="1"
                    Margin="3"/>
            </Grid>

            <!-- Список контактов -->
            <ListBox 
                Grid.Column="0"
                Grid.Row="1"
                ItemsSource="{Binding ContactList}"
                SelectedItem="{Binding SelectedContact}"
                Margin="3"
                ItemTemplate="{StaticResource ContactListTemplate}"
                />

            <!-- Набор кнопок добавления/редактирования/удаления-->
            <Grid
                Grid.Row="2"
                Grid.Column="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>
                <Button
                    Grid.Column="0"
                    Height="25"
                    Margin="3"
                    Content="Add"
                    Command="{Binding AddCommand}"
                    />
                <Button
                    Grid.Column="1"
                    Height="25"
                    Margin="3"
                    Content="Edit"
                    Command="{Binding EditCommand}"
                    CommandParameter="{Binding SelectedContact}"
                    />
                <Button
                    Grid.Column="2"
                    Height="25"
                    Margin="3"
                    Content="Remove"
                    Command="{Binding RemoveCommand}"
                    CommandParameter="{Binding SelectedContact}"
                    />
            </Grid>
        </Grid>
        <!-- Правая колонка -->
        <StackPanel
            Grid.Column="1"
            Margin="8">

            <!-- ContactEditControl -->
            <uc:ContactControl/>

            <!-- Кнопка подтверждения -->
            <StackPanel
                Grid.Column="1"
                Grid.Row="1"
                Orientation="Horizontal"
                HorizontalAlignment="Left"
                Margin="130, 0, 0, 0">
                <Button 
                    Content="Apply"
                    Height="25"
                    Width="75" 
                    Margin="3"
                    Command="{Binding ApplyCommand}" 
                    AutomationProperties.Name="ApplyButton"
                    />
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
